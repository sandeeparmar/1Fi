{
  "name": "onefi-marketplace-assignment",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "server": "node server/server.js",
    "client": "npm --prefix client run dev",
    "build": "npm --prefix client run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "mongoose": "^8.9.5"
  }
}
